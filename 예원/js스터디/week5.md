# WEEK 5

## 10-1

### 1.

- 3번

### 2.

- 2번

### 3.

- 1번

### 4.

```
    class App React.Component{
        constructor (props)
        super(props)
        this.state = { seconde = 0 }
        this.Change = this.Change.value(this)
    }

    Change (event){
        if(event.target.checked){
            this.timerID
        }
    }


    return (){
    <div>
    <input type ='checkbox' onchange = {this.hadleChange} />
    <span>타이머 활성화</span>
    <h1>{this.state.seconds}</h1>
    </div>
    }
```

### 5.

```
class App React.Component{
        constructor (props)
        super(props)
        this.state ={
            input = 0,
            output = 0,
            scale = 10
        }

        handleInput(event) {
            const output = (evnet.target.value )
        }
    }
```

### 6.
